/* eslint-disable no-undef */
describe('Initial test for test', () => {
  test('Should return 4', () => {
    const actual = 4;
    const expected = 4;
    expect(actual).toEqual(expected);
  });
});
